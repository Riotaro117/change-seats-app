import ClassroomButton from '../ui/ClassroomButton';

const MainContents = () => {
  // 画面表示の切り替え
  return (
    <main className="max-w-7xl mx-auto px-4 pt-6">
        <>
          <ClassroomButton/>
        </>
    </main>
  );
};

export default MainContents;
